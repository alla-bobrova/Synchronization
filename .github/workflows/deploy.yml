name: Deploy Streamlit App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2.5.0  # Обновленная версия

    - name: Setup Python
      uses: actions/setup-python@v2.3.1  # Обновленная версия
      with:
        python-version: 3.8

    # Остальные шаги вашего workflow...
